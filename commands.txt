BACKEND COMMANDS
python -m venv env(setup virtual environment)
pip install -r requirements.txt
python manage.py runserver (start server)
set aws credentials in your environment variable on windows.
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY



